{
	"name": "obsidian-vault-bridges",
	"version": "0.1.0",
	"description": "Link external Git repositories into your Obsidian vault via managed symlinks.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"deploy": "npm run build && cp main.js manifest.json styles.css \"/Users/rickbowman/Documents/Personal/.obsidian/plugins/vault-bridges/\"",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"typecheck": "tsc --noEmit"
	},
	"keywords": ["obsidian", "plugin", "git", "symlink", "sync"],
	"author": {
		"name": "Rick Bowman",
		"url": "https://github.com/richardbowman"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.10.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.19.8",
		"obsidian": "^1.4.16",
		"tslib": "^2.6.2",
		"typescript": "^5.3.3"
	}
}
